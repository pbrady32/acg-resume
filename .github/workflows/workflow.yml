on:
  push:
    branches:
    - master

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - uses: actions-hub/gcloud@master
        env:
          GCS_BUCKET: ${{ secrets.GCS_BUCKET }}
          PROJECT_ID: ${{ secrets.GCS_PROJECT }}
          APPLICATION_CREDENTIALS: ${{ secrets.GCS_SA_KEY }}
        with:
          args: rsync -R resume_challenge gs://$GCS_BUCKET/
          cli: gsutil
